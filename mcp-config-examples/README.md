# Cascade MCP Server Setup

This directory contains examples for connecting editors to the Cascade MCP Server.

**Important:** The MCP server requires `mcp` and `httpx` to be installed in your **global** Python environment, as it runs outside the Cascade backend virtual environment.

```bash
python -m pip install mcp httpx
```

See the files in this directory for specific editor instructions.
